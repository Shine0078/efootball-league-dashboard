import fs from "fs"; const L = []; const w = s => L.push(s); w("import { describe, it } from \"node:test\";"); w("import assert from \"node:assert/strict\";"); w("import {"); w("  generateKnockoutBracket,"); w("  knockoutRounds,"); w("  knockoutTotalMatches,"); w("  roundName,"); w("  generateFixturesForAll,"); w("  generateFixturesForPlayer,"); w("} from \"@/lib/fixtures\";"); w("import { determineWinner, buildBracketView } from \"@/lib/knockout\";"); w(""); fs.writeFileSync("C:/Users/samue/efootball-league-dashboard/tests/knockout.test.ts", L.join(String.fromCharCode(10)), "utf-8"); console.log("Part1 OK, lines:" + L.length);
w("test2");
